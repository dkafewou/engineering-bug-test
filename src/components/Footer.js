import React from 'react';
import '../styles/App.css';

// Fix typo in word extends
// Make this a functional component since it does not hold any state nor perform any action
const Footer = ({}) => (
  <div>
    <div className='footer-anchor'></div>
    <div className='app-footer'></div>
  </div>
)

export default Footer;
